{
    "version": "0.2.0",

    "configurations": [
    
        {
            "type": "node-terminal",
            "name": "Run",
            "request": "launch",
            "command": "npm run debug",
            "cwd": "${workspaceFolder}"
          }
    ]
}